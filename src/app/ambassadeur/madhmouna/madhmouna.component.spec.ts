/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { MadhmounaComponent } from './madhmouna.component';

describe('MadhmounaComponent', () => {
  let component: MadhmounaComponent;
  let fixture: ComponentFixture<MadhmounaComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [MadhmounaComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(MadhmounaComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
